# Use when Railway "Root Directory" = broker/ (Railway auto-detects Dockerfile in that folder).
# Uses Dockerfile.broker-root (build context = broker/).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.broker-root"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
