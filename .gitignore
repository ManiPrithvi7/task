# MongoDB dumps — may contain OAuth tokens and PII; keep local only
backups/

# Private key material — NEVER commit. Use env/base64 injection.
*.key
*.crt
*.p12
*.pfx
*.key.der
node_modules/
test-suite/
dist/
graphify-out/
session-*.md
.env
*.env
strix_runs/
# Local usage traces (runtime/debug exports — do not commit)
influx_usage.csv
data/influx_usage.csv
redis_usage.csv
data/redis_usage.csv
data/leak-hunter.jsonl
data/leak-hunter-latest.json
